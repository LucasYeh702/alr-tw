[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alr-tw"
version = "0.12.0"
description = "Agent-neutral legal research verification runtime for Taiwan law."
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2",
  "pyyaml>=6",
]

[project.scripts]
alr-tw = "alr_tw.cli:main"
alr-tw-demo = "alr_tw.harness.orchestrator:main"
alr-tw-boundary-check = "alr_tw.scripts.check_public_boundary:main"
alr-tw-mcp = "alr_tw.mcp.server:main"
alr-tw-chronolex = "alr_tw.evaluation.chronolex_tw:main"
tw-legal-rag-mcp = "tw_legal_rag_mcp.mcp_server.server:main"

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff==0.15.19",
  "mypy>=1.10",
]
mcp = [
  "mcp>=1.0",
]
live = [
  "httpx>=0.27",
  "truststore>=0.10",
  "beautifulsoup4>=4.12",
  "lxml>=5",
  "platformdirs>=4",
]
tlr = [
  "httpx>=0.27",
  "truststore>=0.10",
]
all = [
  "mcp>=1.0",
  "httpx>=0.27",
  "truststore>=0.10",
  "beautifulsoup4>=4.12",
  "lxml>=5",
  "platformdirs>=4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
